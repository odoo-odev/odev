--- HELP FOR assets ---
[i] Loading commands
ODEV ASSETS

Delete assets files from the database.
Files will be re-generated by odoo-bin on the next run of the database.

Usage: odev assets [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}]
[-h]
  [-f] [-c] [-V VERSION] [--venv VENV] [-w WORKTREE] [--no-pretty]
  database [addons]

Positional Arguments:

    database    The database to target.
    addons      Comma-separated list of additional addon paths.
                The standard Odoo addons paths are automatically added to the
odoo-bin command (including enterprise
                if any enterprise module is installed). If this command is run
from within an Odoo addons directory
                and no additional addons are specified, the current directory
will be added to the list of addons.

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
    -c, --community    Force running the database without enterprise addons.
    -V, --version      The Odoo version to use for running the database.
                       If not specified, defaults to the latest version of the
base module installed in the database.
    --venv             Name of the Python virtual environment to use when
running this database.
                       If not specified, defaults to the common virtual
environment for the current Odoo version.
    -w, --worktree     Name of the worktree to use when running this database.
                       If not specified, defaults to the common worktree for the
current Odoo version.
    --no-pretty        Do not pretty print the output of odoo-bin but rather
display logs as output by the subprocess.
--- HELP FOR cloc ---
[i] Loading commands
ODEV CLOC

Run odoo-bin cloc on a database and count custom lines of code within the
installed modules.

Usage: odev cloc [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}] [-h]
[-f] [-c] [-V VERSION] [--venv VENV] [-w WORKTREE] [--no-pretty]
[--csv]
database [addons]

Positional Arguments:

    database    The database to target.
    addons      Comma-separated list of additional addon paths.
                The standard Odoo addons paths are automatically added to the
odoo-bin command (including enterprise
                if any enterprise module is installed). If this command is run
from within an Odoo addons directory
                and no additional addons are specified, the current directory
will be added to the list of addons.

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
    -c, --community    Force running the database without enterprise addons.
    -V, --version      The Odoo version to use for running the database.
                       If not specified, defaults to the latest version of the
base module installed in the database.
    --venv             Name of the Python virtual environment to use when
running this database.
                       If not specified, defaults to the common virtual
environment for the current Odoo version.
    -w, --worktree     Name of the worktree to use when running this database.
                       If not specified, defaults to the common worktree for the
current Odoo version.
    --no-pretty        Do not pretty print the output of odoo-bin but rather
display logs as output by the subprocess.
    --csv              Format output as CSV.
--- HELP FOR clone ---
[i] Loading commands
ODEV CLONE

Clone a GitHub repository locally, under the path managed by odev. A database
name can be passed instead
of a repository address to find and clone the repository linked to that
database.

Usage: odev clone [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}] [-h]
 [-f] [-p {saas,paas}] [-b BRANCH]
 [database] [repository]

Positional Arguments:

    database      The database to target.
    repository    GitHub URL or name of a repository.

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
    -p, --platform     Force searching for the database on the specified
platform, useful when
                       different databases have the same name on different
hosting (usually one
                       local database being a copy of a remote one).
                       One of saas or paas.
    -b, --branch       Branch to checkout after cloning the repository.
--- HELP FOR config ---
[i] Loading commands
ODEV CONFIG

Get or set configuration values.

Usage: odev config [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}]
[-h]
  [-f]
  [key] [value]

Aliases: conf, cfg

Positional Arguments:

    key      Configuration key to fetch, in the format 'section.option'.
    value    Value to set for the given key.

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
--- HELP FOR create ---
[i] Loading commands
ODEV CREATE

Create a new Odoo database locally, or copy an existing database template.

Usage: odev create [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}]
[-h]
  [-f] [-c] [-V VERSION] [--venv VENV] [-w WORKTREE] [--no-pretty]
  [-t FROM_TEMPLATE] [-T] [--no-filestore] [--bare]
  database [addons]

Aliases: cr

Positional Arguments:

    database    The database to target.
    addons      Comma-separated list of additional addon paths.
                The standard Odoo addons paths are automatically added to the
odoo-bin command (including enterprise
                if any enterprise module is installed). If this command is run
from within an Odoo addons directory
                and no additional addons are specified, the current directory
will be added to the list of addons.

Optional Arguments:

    -v, --log-level          Set logging verbosity for the execution of odev.
    -h, --help               Show help for the current command.
    -f, --force              Bypass confirmation prompts and assume a default
value to all, use with caution!
    -c, --community          Force running the database without enterprise
addons.
    -V, --version            The Odoo version to use for the new database.
                             If not specified and a template is provided, the
version of
                             the template database will be used. Otherwise, the
version will default to "master".
    --venv                   Name of the Python virtual environment to use when
running this database.
                             If not specified, defaults to the common virtual
environment for the current Odoo version.
    -w, --worktree           Name of the worktree to use when running this
database.
                             If not specified, defaults to the common worktree
for the current Odoo version.
    --no-pretty              Do not pretty print the output of odoo-bin but
rather display logs as output by the subprocess.
    -t, --from-template      Name of an existing PostgreSQL database to copy
instead of initializing a new Odoo database.
                             If passed without a value (empty string), search
for a template database with the same name as the new database.
    -T, --create-template    Create the database as a template to reuse later
(append ':template' to its name).
    --no-filestore           Do not copy the filestore from the template.
    --bare                   Do not initialize the database (create the
PostgreSQL database then exit).
--- HELP FOR delete ---
[i] Loading commands
ODEV DELETE

Remove a local PostgreSQL database and its associated resources.
If no database is provided, prune all databases that are not whitelisted.

Usage: odev delete [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}]
[-h]
  [-f] [-k KEEP] [-e EXPRESSION] [-w]
  [database]

Aliases: remove, rm

Positional Arguments:

    database    The database to target.

Optional Arguments:

    -v, --log-level              Set logging verbosity for the execution of
odev.
    -h, --help                   Show help for the current command.
    -f, --force                  Bypass confirmation prompts and assume a
default value to all, use with caution!
    -k, --keep                   List of associated resources to keep, separated
by commas. Possible values are:
                                 - filestore: keep the database filestore
                                 - venv: keep the virtual environment associated
to the database
                                 - config: keep saved attributes for the
database (i.e. whitelist, saved arguments,...)
    -e, --expression             Regular expression pattern to filter databases
to delete.
                                 Ignored if a database was provided.
    -w, --include-whitelisted    Delete whitelisted databases as well.
--- HELP FOR deploy ---
[i] Loading commands
ODEV DEPLOY

Deploy a local module to a database through the module import feature.
Requires the 'base_import_module' module to be installed on the database,
and the database to be running.

Usage: odev deploy [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}]
[-h]
  [-f] [-p {local,remote}] [-b BRANCH]
  database module

Positional Arguments:

    database    The database to target.
    module      Path to the module to deploy (must be a valid Odoo module).

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
    -p, --platform     Force searching for the database on the specified
platform, useful when
                       different databases have the same name on different
hosting (usually one
                       local database being a copy of a remote one).
                       One of local or remote.
    -b, --branch       The branch to target, only used with PaaS (Odoo SH)
databases
                       to force using a specific branch after project detection.
--- HELP FOR dump ---
[i] Loading commands
ODEV DUMP

Backup a database and save its dump file on the local filesystem.

Usage: odev dump [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}] [-h]
[-f] [-p {local,saas,paas,remote}] [-b BRANCH] [-F]
database

Positional Arguments:

    database    The database to target.

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
    -p, --platform     Force searching for the database on the specified
platform, useful when
                       different databases have the same name on different
hosting (usually one
                       local database being a copy of a remote one).
                       One of local, saas, paas or remote.
    -b, --branch       The branch to target, only used with PaaS (Odoo SH)
databases
                       to force using a specific branch after project detection.
    -F, --filestore    Include the filestore when downloading the database.
--- HELP FOR fetch ---
[i] Loading commands
ODEV FETCH

Fetch changes in local Odoo worktrees managed by odev.

Usage: odev fetch [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}] [-h]
 [-f] [-V VERSION] [-w [WORKTREE]]

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
    -V, --version      Act on a specific Odoo version only.
    -w, --worktree     Name of a specific worktree to fetch changes for.
--- HELP FOR help ---
[i] Loading commands
ODEV HELP

Display extensive help about the selected command or a generic help message
lightly covering all available commands.

Usage: odev help [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}] [-h]
[-f] [-1]
[command]

Aliases: h, -h, --help

Positional Arguments:

    command    Get help about a specific command.
               Use odev help for a list of available commands.

Optional Arguments:

    -v, --log-level                   Set logging verbosity for the execution of
odev.
    -h, --help                        Show help for the current command.
    -f, --force                       Bypass confirmation prompts and assume a
default value to all, use with caution!
    -1, --names-only, --one-column    List command names one per line - useful
for parsing
--- HELP FOR history ---
[i] Loading commands
ODEV HISTORY

Check the history of odev commands run in the past.

Usage: odev history [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}]
[-h]
   [-f] [-c COMMAND] [-C]

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
    -c, --command      The command to check the history of.
    -C, --clear        Clear the history.
--- HELP FOR info ---
[i] Loading commands
ODEV INFO

Fetch and display information about a database.

Usage: odev info [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}] [-h]
[-f] [-p {local,saas,paas}] [-b BRANCH]
database

Aliases: i

Positional Arguments:

    database    The database to target.

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
    -p, --platform     Force searching for the database on the specified
platform, useful when
                       different databases have the same name on different
hosting (usually one
                       local database being a copy of a remote one).
                       One of local, saas or paas.
    -b, --branch       The branch to target, only used with PaaS (Odoo SH)
databases
                       to force using a specific branch after project detection.
--- HELP FOR kill ---
[i] Loading commands
ODEV KILL

Kill a running Odoo database. Useful if the process crashed because of
a forgotten IPDB or if you lost your terminal and don't want to search
for the process' PID.

Usage: odev kill [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}] [-h]
[-f] [-H]
database

Positional Arguments:

    database    The database to target.

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
    -H, --hard         Kill the database process with SIGKILL instead of SIGINT.
--- HELP FOR list ---
[i] Loading commands
ODEV LIST

List local Odoo databases with their current status and information required for
their management through this
tool. This includes custom repositories, virtual environments, worktrees, sizes,
last used dates, and whether they
are whitelisted or not.

By default, only Odoo databases are listed. Use the `--all` option to include
non-Odoo databases or templates
generated through `odev create` and `odev run` as well.

System databases such as `postgres`, `template0`, `template1`, `odev`, and the
default user database are always
excluded from the list.

Usage: odev list [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}] [-h]
[-f] [-1] [-e EXPRESSION] [-a]
[-s {name,version,size,size_fs,date,venv,worktree,repository,pid,whitelisted}]

Aliases: ls

Optional Arguments:

    -v, --log-level     Set logging verbosity for the execution of odev.
    -h, --help          Show help for the current command.
    -f, --force         Bypass confirmation prompts and assume a default value
to all, use with caution!
    -1, --names-only    List database names one per line - useful for parsing.
    -e, --expression    Regular expression pattern to filter listed databases.
    -a, --all           Show non-Odoo databases and templates.
    -s, --sort          Sort databases by their value in one of the columns
displayed.
                        Possible values are name, version, size, size_fs, date,
venv, worktree, repository, pid and whitelisted.
--- HELP FOR neutralize ---
[i] Loading commands
ODEV NEUTRALIZE

Neutralize a local database and make it suitable for development without risking
sending emails or running
undesired actions.

Usage: odev neutralize [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}]
      [-h] [-f]
      database

Aliases: clean, cl

Positional Arguments:

    database    The database to target.

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
--- HELP FOR pathfinder ---
[i] Loading commands
ODEV PATHFINDER

Find the shortest path between two models in a database using a BFS algorithm.

Usage: odev pathfinder [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}]
      [-h] [-f] [-c] [-V VERSION] [--venv VENV] [-w WORKTREE]
      [--no-pretty]
      database [addons] origin destination

Aliases: pf

Positional Arguments:

    database       The database to target.
    addons         Comma-separated list of additional addon paths.
                   The standard Odoo addons paths are automatically added to the
odoo-bin command (including enterprise
                   if any enterprise module is installed). If this command is
run from within an Odoo addons directory
                   and no additional addons are specified, the current directory
will be added to the list of addons.
    origin         Model to start from.
    destination    Model to end at.

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
    -c, --community    Force running the database without enterprise addons.
    -V, --version      The Odoo version to use for running the database.
                       If not specified, defaults to the latest version of the
base module installed in the database.
    --venv             Name of the Python virtual environment to use when
running this database.
                       If not specified, defaults to the common virtual
environment for the current Odoo version.
    -w, --worktree     Name of the worktree to use when running this database.
                       If not specified, defaults to the common worktree for the
current Odoo version.
    --no-pretty        Do not pretty print the output of odoo-bin but rather
display logs as output by the subprocess.
--- HELP FOR plugin ---
[i] Loading commands
ODEV PLUGIN

Enable and disable plugins to add new features and commands.

Usage: odev plugin [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}]
[-h]
  [-f] [-e] [-d] [-s]
  [plugin]

Aliases: plugins

Positional Arguments:

    plugin    Plugin to enable or disable, must be a git repository hosted on
GitHub.
              Use format <organization>/<repository>.
              If `--show` is used and no plugin is provided, show the state of
all plugins.

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
    -e, --enable       Download and enable an inactive plugin.
    -d, --disable      Disable an active plugin.
    -s, --show         Show the state of a plugin and its description if
available.
--- HELP FOR pull ---
[i] Loading commands
ODEV PULL

Pull changes in local worktrees managed by odev.

Usage: odev pull [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}] [-h]
[-f] [-V VERSION] [-w [WORKTREE]]

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
    -V, --version      Act on a specific Odoo version only.
    -w, --worktree     Name of a specific worktree to fetch changes for.
--- HELP FOR quickstart ---
[i] Loading commands
ODEV QUICKSTART

Dump, restore and neutralize an existing database so that it can be used
as a starting point for new custom developments.
If possible, clone the repository containing existing customizations for
the selected database.

Usage: odev quickstart [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}]
      [-h] [-f] [-p {saas,paas}] [-b BRANCH] [-n NAME]
      [-V VERSION] [-F]
      [--cli {claude,gemini,copilot,opencode-cli}] [-D]
      [--depends DEPENDS] [-d DIRS] [--format {xml,py}] [-H]
      [--model MODEL] [-m MODULE] [-M] [--path PATH]
      [--resume RESUME] [-t] [-y]
      [database] [task_id]

Aliases: qs

Positional Arguments:

    database    The database to target.
    task_id     Odoo or Ps-tools task_id

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
    -p, --platform     Force searching for the database on the specified
platform, useful when
                       different databases have the same name on different
hosting (usually one
                       local database being a copy of a remote one).
                       One of saas or paas.
    -b, --branch       Branch to target for downloading a backup of PaaS (Odoo
SH) instances.
                       Also used as the branch to checkout after cloning the
repository containing
                       code customization for the selected database.
    -n, --name         The name of the database to create locally, defaults to
the name of the database dumped.
    -V, --version      The Odoo version to use for the new database. If
specified, a new database
                       will be created from scratch instead of downloading and
restoring a backup.
    -F, --filestore    Include the filestore when downloading a backup of the
database.
    --cli              The CLI AI agent to use (claude, gemini, copilot, or
opencode-cli).
    -D, --demo         Include the generation of demo data files (CSV/XML) for
the module.
    --depends          Comma separated value for the modules files used as
context
    -d, --dirs         Comma-separated list of extra directories to include in
the sandbox (read-write).
    --format           The output format (importable or python module).
    -H, --headless     Run the AI agent in non-interactive (headless) mode.
    --model            The specific model to use for the chosen AI agent.
    -m, --module       Existing module name to override
    -M                 Do not migrate the manual / studio fields into python
fields.
    --path             Path to the export template.
    --resume           Resume a previous AI session by ID or 'latest'.
    -t, --tests        Generate standard Odoo unit tests based on the task
requirements.
    -y, --yolo         Automatically accept all AI commands (YOLO mode).
--- HELP FOR rename ---
[i] Loading commands
ODEV RENAME

Rename a local database and move its filestore to the correct path.

Usage: odev rename [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}]
[-h]
  [-f]
  database name

Aliases: mv

Positional Arguments:

    database    The database to target.
    name        New name for the database.

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
--- HELP FOR restore ---
[i] Loading commands
ODEV RESTORE

Restore a dump file on the local filesystem to a database.
This will effectively replace all data in the target database.

Usage: odev restore [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}]
[-h]
   [-f] [-p {local}] [-b BRANCH] [--no-neutralize]
   database backup

Positional Arguments:

    database    The database to target.
    backup      Path to the backup file to restore.

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
    -p, --platform     Force searching for the database on the specified
platform, useful when
                       different databases have the same name on different
hosting (usually one
                       local database being a copy of a remote one).
                       One of local.
    -b, --branch       The branch to target, only used with PaaS (Odoo SH)
databases
                       to force using a specific branch after project detection.
    --no-neutralize    Do not neutralize the database after the dump has been
restored.
                       Only used on local databases.
--- HELP FOR run ---
[i] Loading commands
ODEV RUN

 Run the odoo-bin process for the selected database locally.
The process is run in a python virtual environment depending on the database's
odoo version (as defined
by the installed `base` module). The command takes care of installing and
updating python requirements within
the virtual environment and fetching the latest sources in the odoo standard
repositories, cloning them
if necessary. All odoo-bin arguments are passed to the odoo-bin process.

 Usage: odev run [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}] [-h]
[-f]
[-c] [-V VERSION] [--venv VENV] [-w WORKTREE] [--no-pretty]
[-t FROM_TEMPLATE] [--ai]
[--cli {claude,gemini,copilot,opencode-cli}] [-d DIRS] [-H]
[--model MODEL] [--resume RESUME] [-y]
database [addons]

 Positional Arguments:

     database    The database to target.
    addons      Comma-separated list of additional addon paths.
                The standard Odoo addons paths are automatically added to the
odoo-bin command (including enterprise
                if any enterprise module is installed). If this command is run
from within an Odoo addons directory
                and no additional addons are specified, the current directory
will be added to the list of addons.

 Optional Arguments:

     -v, --log-level        Set logging verbosity for the execution of odev.
    -h, --help             Show help for the current command.
    -f, --force            Bypass confirmation prompts and assume a default
value to all, use with caution!
    -c, --community        Force running the database without enterprise addons.
    -V, --version          The Odoo version to use for running the database.
                           If not specified, defaults to the latest version of
the base module installed in the database.
    --venv                 Name of the Python virtual environment to use when
running this database.
                           If not specified, defaults to the common virtual
environment for the current Odoo version.
    -w, --worktree         Name of the worktree to use when running this
database.
                           If not specified, defaults to the common worktree for
the current Odoo version.
    --no-pretty            Do not pretty print the output of odoo-bin but rather
display logs as output by the subprocess.
    -t, --from-template    Name of an existing PostgreSQL database to copy
before running.
                           If passed without a value, search for a template
database with the same name as the new database.
    --ai                   Use AI to run Odoo and fix issues if it fails.
    --cli                  The CLI AI agent to use (claude, gemini, copilot, or
opencode-cli).
    -d, --dirs             Comma-separated list of extra directories to include
in the sandbox (read-write).
    -H, --headless         Run the AI agent in non-interactive (headless) mode.
    --model                The specific model to use for the chosen AI agent.
    --resume               Resume a previous AI session by ID or 'latest'.
    -y, --yolo             Automatically accept all AI commands (YOLO mode).
--- HELP FOR setup ---
[i] Loading commands
ODEV SETUP

Re-run odev's setup and allow reconfiguring parts of it.
A category can be provided to run a specific part of the setup only.

Available categories:

    symlink        Setup symlinks to odev for using it as a shell command.

    directories    Setup working directories for odev.

    odoo           Import Odoo repositories if already downloaded.

    update         Configure self-update behavior.

    completion     Setup auto completion script for odev commands and databases.

                   See:
                   - https://github.com/scop/bash-completion/
                   -
https://www.gnu.org/savannah-checkouts/gnu/bash/manual/html_node/Programmable-Co
mpletion.html

    telemetry      Configure self-update behavior.

Usage: odev setup [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}] [-h]
 [-f]
 [{symlink,directories,odoo,update,completion,telemetry}]

Positional Arguments:

    category    Run a specific part of the setup only.

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
--- HELP FOR shell ---
[i] Loading commands
ODEV SHELL

Run the odoo-bin process in shell mode for the selected database locally.

Usage: odev shell [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}] [-h]
 [-f] [-c] [-V VERSION] [--venv VENV] [-w WORKTREE] [--no-pretty]
 [--script SCRIPT]
 database [addons]

Positional Arguments:

    database    The database to target.
    addons      Comma-separated list of additional addon paths.
                The standard Odoo addons paths are automatically added to the
odoo-bin command (including enterprise
                if any enterprise module is installed). If this command is run
from within an Odoo addons directory
                and no additional addons are specified, the current directory
will be added to the list of addons.

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
    -c, --community    Force running the database without enterprise addons.
    -V, --version      The Odoo version to use for running the database.
                       If not specified, defaults to the latest version of the
base module installed in the database.
    --venv             Name of the Python virtual environment to use when
running this database.
                       If not specified, defaults to the common virtual
environment for the current Odoo version.
    -w, --worktree     Name of the worktree to use when running this database.
                       If not specified, defaults to the common worktree for the
current Odoo version.
    --no-pretty        Do not pretty print the output of odoo-bin but rather
display logs as output by the subprocess.
    --script           Run a script inside of odoo-bin shell and exit. Can be a
path
                       to a file containing python code or a string representing
                       python code to be executed inside the shell environment.
--- HELP FOR standardize ---
[i] Loading commands
ODEV STANDARDIZE

Remove customizations from a local Odoo database.
Optionally also remove Studio customizations.

Usage: odev standardize [-v
{CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}]
       [-h] [-f] [-c] [-V VERSION] [--venv VENV] [-w WORKTREE]
       [--no-pretty] [--no-studio]
       database [addons]

Aliases: std, standard

Positional Arguments:

    database    The database to target.
    addons      Comma-separated list of additional addon paths.
                The standard Odoo addons paths are automatically added to the
odoo-bin command (including enterprise
                if any enterprise module is installed). If this command is run
from within an Odoo addons directory
                and no additional addons are specified, the current directory
will be added to the list of addons.

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
    -c, --community    Force running the database without enterprise addons.
    -V, --version      The Odoo version to use for running the database.
                       If not specified, defaults to the latest version of the
base module installed in the database.
    --venv             Name of the Python virtual environment to use when
running this database.
                       If not specified, defaults to the common virtual
environment for the current Odoo version.
    -w, --worktree     Name of the worktree to use when running this database.
                       If not specified, defaults to the common worktree for the
current Odoo version.
    --no-pretty        Do not pretty print the output of odoo-bin but rather
display logs as output by the subprocess.
    --no-studio        Remove Studio customizations.
--- HELP FOR test ---
[i] Loading commands
ODEV TEST

Run unit tests on an empty local Odoo database, creating it on the fly.

Usage: odev test [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}] [-h]
[-f] [-c] [-V VERSION] [--venv VENV] [-w WORKTREE] [--no-pretty]
[-t TESTS] [-i MODULES] [--no-auto-tags] [--ai]
[--cli {claude,gemini,copilot,opencode-cli}] [-d DIRS] [-H]
[--model MODEL] [--resume RESUME] [-y]
database [addons]

Aliases: tests

Positional Arguments:

    database    The database to target.
    addons      Comma-separated list of additional addon paths.
                The standard Odoo addons paths are automatically added to the
odoo-bin command (including enterprise
                if any enterprise module is installed). If this command is run
from within an Odoo addons directory
                and no additional addons are specified, the current directory
will be added to the list of addons.

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
    -c, --community    Force running the database without enterprise addons.
    -V, --version      The Odoo version to use for running the database.
                       If not specified, defaults to the latest version of the
base module installed in the database.
    --venv             Name of the Python virtual environment to use when
running this database.
                       If not specified, defaults to the common virtual
environment for the current Odoo version.
    -w, --worktree     Name of the worktree to use when running this database.
                       If not specified, defaults to the common worktree for the
current Odoo version.
    --no-pretty        Do not pretty print the output of odoo-bin but rather
display logs as output by the subprocess.
    -t, --tags         Comma-separated list of files or tags to run specific
tests.
                       Check
https://www.odoo.com/documentation/17.0/fr/developer/cli.html#testing-configurat
ion
                       for more information on how to use tests.
    -i, --init         Comma-separated list of modules to install for testing.
If not set, install the base module.
    --no-auto-tags     Do not fetch auto-tags from Runbot. By default, auto-tags
are fetched and used to exclude flaky tests.
    --ai               Use AI to run tests and fix failures.
    --cli              The CLI AI agent to use (claude, gemini, copilot, or
opencode-cli).
    -d, --dirs         Comma-separated list of extra directories to include in
the sandbox (read-write).
    -H, --headless     Run the AI agent in non-interactive (headless) mode.
    --model            The specific model to use for the chosen AI agent.
    --resume           Resume a previous AI session by ID or 'latest'.
    -y, --yolo         Automatically accept all AI commands (YOLO mode).
--- HELP FOR update ---
[i] Loading commands
ODEV UPDATE

Force updating Odev and the currently enabled plugins.

Usage: odev update [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}]
[-h]
  [-f]

Aliases: u

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
--- HELP FOR upgrade-code ---
[i] Loading commands
ODEV UPGRADE-CODE

Run Odoo's native upgrade_code tool (Odoo 18.0+).

This command uses Odoo's internal upgrade_code scripts to automatically
refactor source code (e.g., renaming <tree> to <list>).

Usage: odev upgrade-code [-v
{CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}]
        [-h] [-f] [-c] [-V VERSION] [--venv VENV] [-w WORKTREE]
        [--no-pretty] [--from FROM_VERSION] [--to TO_VERSION]
        [--script SCRIPT] [--glob GLOB] [--dry-run]
        database [addons]

Positional Arguments:

    database    The database to target.
    addons      Comma-separated list of additional addon paths.
                The standard Odoo addons paths are automatically added to the
odoo-bin command (including enterprise
                if any enterprise module is installed). If this command is run
from within an Odoo addons directory
                and no additional addons are specified, the current directory
will be added to the list of addons.

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
    -c, --community    Force running the database without enterprise addons.
    -V, --version      The Odoo version to use for running the database.
                       If not specified, defaults to the latest version of the
base module installed in the database.
    --venv             Name of the Python virtual environment to use when
running this database.
                       If not specified, defaults to the common virtual
environment for the current Odoo version.
    -w, --worktree     Name of the worktree to use when running this database.
                       If not specified, defaults to the common worktree for the
current Odoo version.
    --no-pretty        Do not pretty print the output of odoo-bin but rather
display logs as output by the subprocess.
    --from             Run all scripts starting from this version, inclusive.
    --to               Run all scripts until this version, inclusive.
    --script           Run this single script name.
    --glob             Select the files to rewrite (default: **/*).
    --dry-run          Preview changes without writing.
--- HELP FOR venv ---
[i] Loading commands
ODEV VENV

Interact with virtual environments created and managed by Odev.

Usage: odev venv [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}] [-h]
[-f] [-V VERSION] [-c COMMAND] [-l] [-C] [-r] [-s]
[name]

Aliases: virtualenv

Positional Arguments:

    name    Name of the virtual environment to activate, or a local Odoo
database to select
            the virtual environment it is linked to.

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
    -V, --version      Python version to use for the virtual environment.
    -c, --command      Python command to execute in the context of the virtual
environment.
    -l, --list         List virtual environments and their properties.
    -C, --create       Create a new virtual environment.
    -r, --remove       Remove an existing virtual environment.
    -s, --switch       Switch the python version used by an existing virtual
environment.
--- HELP FOR version ---
[i] Loading commands
ODEV VERSION

Print the current version of odev and exit.

Usage: odev version [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}]
[-h]
   [-f] [-p]

Aliases: v

Optional Arguments:

    -v, --log-level       Set logging verbosity for the execution of odev.
    -h, --help            Show help for the current command.
    -f, --force           Bypass confirmation prompts and assume a default value
to all, use with caution!
    -p, --show-plugins
--- HELP FOR worktree ---
[i] Loading commands
ODEV WORKTREE

Manage git worktrees used within odev.

Usage: odev worktree [-v {CRITICAL,ERROR,WARNING,INFO,DEBUG,DEBUG_SQL,NOTSET}]
[-h]
    [-f] [-V VERSION] [-l] [-p] [-C] [-r] [-c]
    [name]

Aliases: wt

Positional Arguments:

    name    Name of the worktree to create, checkout or remove.

Optional Arguments:

    -v, --log-level    Set logging verbosity for the execution of odev.
    -h, --help         Show help for the current command.
    -f, --force        Bypass confirmation prompts and assume a default value to
all, use with caution!
    -V, --version      Act on a specific Odoo version only.
    -l, --list         List worktrees and their properties.
    -p, --prune        Prune worktrees with issues reported by git.
    -C, --create       Create a new worktree.
    -r, --remove       Remove an existing worktree.
    -c, --checkout     Change the revisions used in an existing worktree.
