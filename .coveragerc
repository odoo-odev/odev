[run]
# Trace only odev/ so temp plugin files (e.g. test_16 cycle manifests) are not recorded.
source = odev
# Never traverse the odev/plugins symlink pointing to the installed plugins directory.
omit = odev/plugins/*

[report]
omit = */tests/*
fail_under = 60
show_missing = true
exclude_lines =
    pragma: no cover
    raise NotImplementedError
    raise AssertionError
    if __name__ = .__main__.:
    if TYPE_CHECKING:
